//string concatenation
let firstName = "John";
let lastName = "Doe";
let fullname = firstName + " " + lastName;
console.log(fullname);

//number + string
let numStr = 10 + "5"; // + is a overloaded operator in JS
console.log(numStr);
// string + num
let strNum = "5" - 10; //this give -5 because + is a overloaded operator in JS
console.log(strNum);