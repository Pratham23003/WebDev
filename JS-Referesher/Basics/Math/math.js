// console.log(Math.PI); //logs PI from math object

let v = -10;
let w = 10;
let x = 3.9999;
let y = 2;
let z;

// z = Math.round(x); //rounds the given value
// z = Math.floor(x); // rounds down the given value
// z = Math.ceil(x); //rounds up the given value
// z = Math.trunc(x); //truncates the decimal value
// z = Math.pow(w,y); w^y
// z = Math.sqrt(81);
// z = Math.log(10); 
// z = Math.sign(v); //returns the sign of the number (+ve or -ve)
console.log(z);